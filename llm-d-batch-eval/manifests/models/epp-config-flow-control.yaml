# EPP EndpointPickerConfig with flow-control for EA2 (llm-d-router).
# Used for scenario 4 (aimd-flow-control) to enable priority-based
# request scheduling: interactive (default priority 0) is served before
# batch (priority -1, sheddable).
#
# EA2 auto-registers utilization-detector as the saturation detector.
apiVersion: v1
kind: ConfigMap
metadata:
  name: epp-config-flow-control
  namespace: llm-d-batch
data:
  config.yaml: |
    apiVersion: llm-d.ai/v1alpha1
    kind: EndpointPickerConfig
    featureGates:
    - "flowControl"
    plugins:
    - type: queue-scorer
    - type: kv-cache-utilization-scorer
    - type: prefix-cache-scorer
    - type: precise-prefix-cache-producer
      name: valkey-prefix-producer
      parameters:
        indexerConfig:
          kvBlockIndexConfig:
            redisConfig:
              address: "valkey://valkey.llm-d-batch.svc.cluster.local:6379"
    - type: round-robin-fairness-policy
    - type: global-strict-fairness-policy
    - type: slo-deadline-ordering-policy
    schedulingProfiles:
    - name: default
      plugins:
      - pluginRef: queue-scorer
        weight: 2
      - pluginRef: kv-cache-utilization-scorer
        weight: 2
      - pluginRef: prefix-cache-scorer
        weight: 1
    flowControl:
      maxBytes: 4294967296
      defaultRequestTTL: 30s
      priorityBands:
      - priority: 100
        maxBytes: 1073741824
        fairnessPolicyRef: round-robin-fairness-policy
        orderingPolicyRef: fcfs-ordering-policy
      - priority: -1
        maxBytes: 3221225472
        fairnessPolicyRef: global-strict-fairness-policy
        orderingPolicyRef: slo-deadline-ordering-policy
      defaultPriorityBand:
        maxBytes: 536870912
        fairnessPolicyRef: global-strict-fairness-policy
        orderingPolicyRef: fcfs-ordering-policy
