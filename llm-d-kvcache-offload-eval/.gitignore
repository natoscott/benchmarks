# OpenShift cluster install artifacts (may contain secrets)
install/
install.sh
credentials/

# MLflow credentials (never commit — copy from mlflow.conf.example)
mlflow.conf

# Generated analysis outputs
analysis/
